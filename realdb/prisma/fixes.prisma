// Generated by @ludagg/zeroapi-runtime — do not edit manually
// Spec: realdb-fixes v1.0.0

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Board {
  id            String   @id @default(cuid())
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt
  title         String
  notes         Note[]
}

model Note {
  id            String   @id @default(cuid())
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt
  text          String
  userId        String
  user          User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  boardId       String
  board         Board   @relation(fields: [boardId], references: [id])
}

model Order {
  id            String   @id @default(cuid())
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt
  ref           String
  orderItems    OrderItem[]
}

model Product {
  id            String   @id @default(cuid())
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt
  name          String
  orderItems    OrderItem[]
}

model OrderItem {
  id            String   @id @default(cuid())
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt
  qty           Int
  orderId       String
  order         Order   @relation(fields: [orderId], references: [id])
  prodRef       String
  product       Product   @relation(fields: [prodRef], references: [id])
  @@unique([orderId, prodRef])
}

model Person {
  id            String   @id @default(cuid())
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt
  handle        String
  following     Follows[] @relation("Follows_person")
  followers     Follows[] @relation("Follows_relatedPerson")
}

model Follows {
  personId      String
  relatedPersonId String
  person        Person  @relation("Follows_person", fields: [personId], references: [id])
  relatedPerson Person  @relation("Follows_relatedPerson", fields: [relatedPersonId], references: [id])

  @@id([personId, relatedPersonId])
}

model User {
  id            String    @id @default(uuid())
  email         String    @unique
  passwordHash  String
  salt          String
  role          String    @default("user")
  emailVerified Boolean   @default(false)
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt
  refreshTokens RefreshToken[]
  ownedNotes    Note[]
}

model RefreshToken {
  id        String   @id @default(uuid())
  tokenHash String   @unique
  userId    String
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  expiresAt DateTime
  revoked   Boolean  @default(false)
  createdAt DateTime @default(now())
}
