// Generated by @ludagg/zeroapi-runtime — do not edit manually
// Spec: realdb-e2e v1.0.0

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Todo {
  id            String   @id @default(cuid())
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt
  title         String
  done          Boolean? @default(false)
}

model Author {
  id            String   @id @default(cuid())
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt
  name          String
  comments      Comment[]
}

model Post {
  id            String   @id @default(cuid())
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt
  title         String
  comments      Comment[]
  postHashtags  PostHashtags[]
}

model Comment {
  id            String   @id @default(cuid())
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt
  text          String
  postId        String
  post          Post   @relation(fields: [postId], references: [id])
  authorId      String
  author        Author   @relation(fields: [authorId], references: [id])
}

model Hashtag {
  id            String   @id @default(cuid())
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt
  label         String
  postHashtags  PostHashtags[]
}

model Person {
  id            String   @id @default(cuid())
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt
  handle        String
  follows       Follows[] @relation("Follows_person")
  relatedFollows Follows[] @relation("Follows_relatedPerson")
}

model Product {
  id            String   @id @default(cuid())
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt
  name          String
  stock         Int
  purchases     Purchase[]
}

model Purchase {
  id            String   @id @default(cuid())
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt
  quantity      Int
  productId     String
  product       Product   @relation(fields: [productId], references: [id])
}

model PostHashtags {
  postId        String
  hashtagId     String
  post          Post  @relation(fields: [postId], references: [id])
  hashtag       Hashtag  @relation(fields: [hashtagId], references: [id])

  @@id([postId, hashtagId])
}

model Follows {
  personId      String
  relatedPersonId String
  person        Person  @relation("Follows_person", fields: [personId], references: [id])
  relatedPerson Person  @relation("Follows_relatedPerson", fields: [relatedPersonId], references: [id])

  @@id([personId, relatedPersonId])
}
